const initState = {
    username: ''
}


export function authReducer(state = initState, action) {
    switch (action.type) {
        case 'SET_USER':

        default:
            return state;
    }
}